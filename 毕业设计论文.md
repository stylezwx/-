# 基于 STM32 与蓝牙物联网的智能宠物烘干箱设计与实现

**学　　院：** XXXX 学院
**专　　业：** 电子信息工程 / 物联网工程
**学　　号：** XXXXXXXXXXXX
**姓　　名：** XXX
**指导教师：** XXX
**完成日期：** 2026 年 6 月

---

## 摘　要

随着家庭养宠数量的持续增长，宠物清洁护理日益成为一项高频的家庭需求。传统宠物烘干方式存在温度不可控、烘干不均匀、缺乏安全保护以及无法远程操作等问题，容易造成宠物应激甚至烫伤。针对上述问题，本文设计并实现了一套以 STM32F103C8T6 微控制器为核心、融合蓝牙低功耗（BLE）通信与 Web 上位机的智能宠物烘干箱系统。

系统在硬件上以 STM32F103C8T6 最小系统为主控，集成双路 DHT11 温湿度传感器实现箱体内多点温度采集，采用基于 TIM3 的 25 kHz PWM 对直流风机进行无级调速，通过继电器实现加热回路的通断与过温保护，利用 0.96 寸 OLED 显示运行状态，配合按键、蜂鸣器与状态指示灯完成人机交互与声光报警。为保障宠物安全，系统引入红外/称重式在位检测传感器，一旦烘干过程中宠物离开箱体超过设定时间，立即停机并报警。在通信与控制层面，系统通过 USART1 实现命令解析与遥测输出，并设计了一套面向"宠物种类—体型"的预设烘干模式协议，包含小型犬、中型犬、大型犬、小型猫、中型猫、大型猫共六种模式，每种模式自动配置烘干时长、报警温度与风机转速。

为实现移动端远程控制，系统采用 ESP32-S3 模组在 ESP-IDF 框架下基于 NimBLE 协议栈构建 BLE GATT 服务器，手机 App 通过蓝牙写入指令，ESP32-S3 经由 USB 串口将指令透传给上位机。上位机基于浏览器 Web Serial API 与 Node.js 本地服务实现，可同时连接 STM32 与 ESP32 两路串口，完成遥测数据的实时解析与可视化展示、远程指令下发，并将解析后的温湿度、倒计时、报警温度等数据自动写入 MySQL 数据库，形成可追溯的运行日志。

测试结果表明，系统温度采集误差在 ±2 ℃ 以内，PWM 调速线性度良好，倒计时计时精度优于 0.5%，蓝牙通信稳定可靠，上位机数据入库成功率高。该系统结构清晰、功能完整、扩展性强，达到了智能化、安全化、可远程监控的设计目标，具有一定的实用价值与推广前景。

**关键词：** STM32；宠物烘干箱；蓝牙低功耗（BLE）；DHT11；PWM 调速；Web Serial；物联网

---

## Abstract

With the continuous growth in the number of household pets, pet cleaning and grooming has become an increasingly frequent domestic demand. Traditional pet drying methods suffer from uncontrollable temperature, uneven drying, lack of safety protection, and the inability to operate remotely, which may cause stress or even burns to pets. To address these problems, this thesis designs and implements an intelligent pet drying box system centered on the STM32F103C8T6 microcontroller and integrated with Bluetooth Low Energy (BLE) communication and a web-based host computer.

In terms of hardware, the system uses the STM32F103C8T6 minimum system as the main controller, integrates dual DHT11 temperature and humidity sensors for multi-point temperature acquisition inside the box, employs a 25 kHz PWM signal based on TIM3 for stepless speed regulation of the DC fan, controls the heating loop and over-temperature protection through a relay, and displays the operating status on a 0.96-inch OLED, together with keys, a buzzer, and a status LED for human-machine interaction and audible/visual alarms. To ensure pet safety, an infrared/weight presence sensor is introduced; once the pet leaves the box for longer than a preset time during drying, the system immediately stops and raises an alarm. At the communication and control level, command parsing and telemetry output are realized through USART1, and a "pet-species/body-size" preset drying mode protocol is designed, including six modes (small/medium/large dog and small/medium/large cat), each automatically configuring drying duration, alarm temperature, and fan speed.

To realize mobile remote control, the system uses an ESP32-S3 module to build a BLE GATT server based on the NimBLE stack under the ESP-IDF framework. A mobile App writes commands via Bluetooth, and the ESP32-S3 transparently forwards them to the host computer through a USB serial port. The host computer, implemented with the browser Web Serial API and a Node.js local service, can connect to both the STM32 and ESP32 serial ports simultaneously, performing real-time parsing and visualization of telemetry data, remote command issuing, and automatically writing parsed data such as temperature, humidity, countdown, and alarm temperature into a MySQL database to form a traceable operation log.

Test results show that the temperature acquisition error is within ±2 ℃, the PWM speed regulation exhibits good linearity, the countdown timing accuracy is better than 0.5%, the Bluetooth communication is stable and reliable, and the data storage success rate of the host computer is high. The system has a clear structure, complete functions, and strong scalability, achieving the design goals of intelligence, safety, and remote monitoring, and has certain practical value and promotion prospects.

**Keywords:** STM32; Pet drying box; Bluetooth Low Energy (BLE); DHT11; PWM speed regulation; Web Serial; Internet of Things

---

## 目　录

- 第 1 章 绪论
  - 1.1 课题研究背景与意义
  - 1.2 国内外研究现状
  - 1.3 本文主要研究内容
  - 1.4 论文组织结构
- 第 2 章 系统需求分析与总体方案设计
  - 2.1 系统需求分析
  - 2.2 系统总体架构
  - 2.3 关键技术选型与论证
  - 2.4 系统工作流程
- 第 3 章 系统硬件设计
  - 3.1 主控最小系统
  - 3.2 温湿度采集模块
  - 3.3 OLED 显示模块
  - 3.4 PWM 风机驱动模块
  - 3.5 继电器加热控制模块
  - 3.6 蜂鸣器与指示灯模块
  - 3.7 按键输入模块
  - 3.8 宠物在位检测模块
  - 3.9 ESP32-S3 蓝牙模块
  - 3.10 电源模块
  - 3.11 硬件资源分配
- 第 4 章 下位机软件设计
  - 4.1 软件总体架构与开发环境
  - 4.2 主程序设计
  - 4.3 软件时间基准与任务调度
  - 4.4 DHT11 单总线驱动设计
  - 4.5 PWM 调速与风机控制
  - 4.6 继电器温度闭环控制
  - 4.7 烘干模式与倒计时管理
  - 4.8 宠物离位安全监测
  - 4.9 按键扫描与消抖
  - 4.10 串口命令协议与解析
  - 4.11 OLED 显示设计
- 第 5 章 蓝牙通信与上位机软件设计
  - 5.1 ESP32-S3 蓝牙接收端设计
  - 5.2 GATT 服务与特征设计
  - 5.3 Web Serial 上位机设计
  - 5.4 Node.js 后端服务与数据持久化
  - 5.5 多通道串口与命令转发
- 第 6 章 系统调试与测试
  - 6.1 测试环境与方法
  - 6.2 硬件功能测试
  - 6.3 温湿度采集测试
  - 6.4 控制功能测试
  - 6.5 蓝牙通信测试
  - 6.6 上位机与数据库测试
  - 6.7 整机联调与结果分析
- 第 7 章 总结与展望
  - 7.1 全文总结
  - 7.2 存在的不足
  - 7.3 工作展望
- 参考文献
- 致谢

---

# 第 1 章 绪论

## 1.1 课题研究背景与意义

### 1.1.1 研究背景

近年来，随着我国居民生活水平的不断提高和家庭结构的变化，饲养宠物的家庭数量呈现持续快速增长的态势。宠物作为家庭重要成员，其日常护理需求日益受到重视，宠物经济（"它经济"）已成为消费市场中一个不可忽视的增长点。在宠物护理的诸多环节中，洗澡后的烘干是一项既频繁又重要的工作。猫狗等宠物的被毛较厚，洗澡后若不能及时彻底干燥，潮湿的毛发环境极易滋生真菌、细菌和螨虫，引发皮肤病；同时，长时间处于潮湿状态还可能导致宠物受凉感冒。因此，安全、高效、舒适的烘干方式对宠物健康至关重要。

传统的宠物烘干主要依赖人工使用吹风机，或采用结构简单的烘干箱。这两种方式都存在明显不足：

第一，**温度不可控、易造成伤害**。普通吹风机和廉价烘干箱缺乏精确的温度检测与闭环控制，出风温度波动大，过高的温度容易烫伤宠物皮肤，引起宠物强烈应激反应；温度过低则烘干效率低下，耗时过长。

第二，**烘干不均匀、效率低**。人工吹风受操作者经验影响大，难以保证全身被毛均匀干燥；而无风速调节的烘干箱往往"一档到底"，无法针对不同体型、不同毛量的宠物自适应调整。

第三，**缺乏安全保护机制**。传统设备通常没有宠物在位检测、过温保护和异常报警功能。一旦宠物在烘干过程中挣脱或离开，设备仍持续工作，存在安全隐患和能源浪费。

第四，**缺乏智能化与远程能力**。绝大多数传统烘干设备只能就地手动操作，无法远程启停、无法实时查看箱内温湿度，更谈不上运行数据的记录与追溯，难以满足现代智能家居场景下用户对便捷性和可视化的需求。

针对上述痛点，将嵌入式控制技术、传感器技术、无线通信技术与上位机数据管理技术相结合，设计一款智能化、安全化、可远程监控的宠物烘干箱，具有重要的现实意义。

### 1.1.2 研究意义

本课题的研究意义主要体现在以下几个方面：

（1）**提升宠物护理的安全性与舒适性**。通过双路温度传感器实时监测箱内温度，配合报警温度阈值与过温保护逻辑，可以有效避免温度过高对宠物造成伤害；引入宠物在位检测与离位报警机制，进一步保障宠物在烘干过程中的安全。

（2）**实现烘干过程的精细化、智能化控制**。系统预置了面向不同宠物种类（猫/犬）和体型（小/中/大）的六种烘干模式，每种模式自动匹配合适的烘干时长、报警温度和风机转速，使烘干过程更加科学、个性化，兼顾效率与舒适。

（3）**探索"嵌入式+蓝牙+Web 上位机"的物联网架构**。本系统将 STM32 下位机、ESP32-S3 蓝牙桥接模块、浏览器 Web Serial 上位机与 MySQL 数据库有机结合，构建了一条从移动端指令到执行机构、再到数据回传与持久化的完整数据链路，对同类小型物联网控制系统的设计具有一定的参考价值。

（4）**具有良好的工程实践与教学价值**。课题涵盖了单片机最小系统设计、传感器驱动、PWM 调速、单总线时序、串口通信协议设计、BLE GATT 服务开发、前端 Web 串口编程、Node.js 后端与数据库操作等多项关键技术，是一次较为完整的软硬件综合训练。

## 1.2 国内外研究现状

### 1.2.1 宠物烘干设备发展现状

从产品形态看，市面上的宠物烘干设备大致经历了三个发展阶段。第一阶段是**手持吹风机**，结构简单、成本低，但完全依赖人工操作，温度和风速不可控，效率与安全性都难以保证。第二阶段是**箱式/笼式烘干机**，将加热、风机封装在密闭箱体内，宠物置于箱中自动烘干，解放了人力，但早期产品多采用机械旋钮控温，控制精度低，且大多缺乏安全保护。第三阶段是**智能烘干设备**，开始引入微控制器、温度传感器、触摸屏甚至 App 控制，能够实现温度闭环、定时、多档风速等功能。

国外品牌在宠物智能护理领域起步较早，部分高端产品已实现温度精确控制、负离子除菌、低噪音等功能，但价格普遍较高。国内市场近年来发展迅速，涌现出大量宠物智能硬件产品，价格亲民、功能日趋丰富，但中低端产品在控制精度、安全保护和数据可视化方面仍有提升空间，尤其是"运行数据可记录、可追溯"这一点，绝大多数消费级产品尚未涉及。

### 1.2.2 相关技术研究现状

**嵌入式控制方面**，以 STM32 为代表的 ARM Cortex-M 系列微控制器凭借高性价比、丰富的片上外设和成熟的开发生态，已成为中小型智能硬件的主流选择。基于 STM32 的恒温控制、电机调速、人机交互等技术方案非常成熟，为本课题提供了坚实的技术基础。

**无线通信方面**，蓝牙低功耗（Bluetooth Low Energy, BLE）因其功耗低、成本低、与智能手机天然兼容等优点，在可穿戴设备、智能家居等近距离物联网场景中得到广泛应用。Espressif 公司的 ESP32 系列芯片集成了 Wi-Fi 与蓝牙，配套的 ESP-IDF 开发框架提供了 Bluedroid 与 NimBLE 两套蓝牙协议栈，其中 NimBLE 资源占用更小，适合 BLE 专用场景。

**上位机与数据可视化方面**，传统上位机多采用 C#、Qt、LabVIEW 等桌面开发技术。近年来，随着 Web 技术的发展，浏览器端新增的 Web Serial API 使得网页可以直接访问本地串口设备，无需安装专用客户端，配合 Node.js 后端即可实现数据采集、可视化与数据库存储，具有跨平台、部署轻量、易于维护的优势，是上位机开发的一种新趋势。

综合来看，将成熟的 STM32 嵌入式控制、BLE 无线桥接与新兴的 Web Serial 上位机技术结合，应用于宠物烘干这一具体场景，并补足"安全保护"与"数据可追溯"两块短板，正是本课题的切入点与创新点所在。

## 1.3 本文主要研究内容

本文围绕"智能、安全、可远程监控"的设计目标，完成了一套完整的智能宠物烘干箱系统的设计与实现，主要研究内容包括：

（1）**系统总体方案设计**。在需求分析的基础上，确定系统采用"STM32 下位机 + ESP32-S3 蓝牙桥 + Web 上位机 + MySQL 数据库"的总体架构，明确各模块功能划分与数据流向。

（2）**硬件电路设计**。完成以 STM32F103C8T6 为核心的主控最小系统设计，以及双路 DHT11 温湿度采集、OLED 显示、PWM 风机驱动、继电器加热控制、蜂鸣器报警、按键输入、宠物在位检测等外围模块的硬件设计，合理分配 MCU 引脚资源。

（3）**下位机软件设计**。基于标准外设库与寄存器混合编程，实现 DHT11 单总线驱动、TIM3 PWM 调速、TIM2 软件时间基准、按键扫描消抖、串口命令协议解析、温度闭环控制、倒计时与多模式管理、宠物离位安全监测以及 OLED 状态显示等功能。

（4）**蓝牙通信设计**。基于 ESP-IDF 与 NimBLE 协议栈，在 ESP32-S3 上构建 BLE GATT 服务器，设计接收（写）特征与发送（通知）特征，实现手机指令的接收、回显与向上位机的串口透传。

（5）**上位机软件设计**。基于浏览器 Web Serial API 实现 STM32 与 ESP32 双串口连接、遥测数据实时解析与可视化、远程指令下发；基于 Node.js 实现本地 HTTP 服务、配置管理与 MySQL 数据入库，并实现 ESP32 蓝牙指令向 STM32 的自动转发。

（6）**系统调试与测试**。搭建测试平台，对各功能模块及整机进行功能测试与性能测试，分析测试结果并验证系统设计目标的达成情况。

## 1.4 论文组织结构

全文共分为七章，各章主要内容如下：

第 1 章 绪论。阐述课题的研究背景与意义，分析国内外研究现状，概述本文主要研究内容与论文组织结构。

第 2 章 系统需求分析与总体方案设计。从功能性与非功能性两方面进行需求分析，给出系统总体架构，对关键技术进行选型论证，并描述系统整体工作流程。

第 3 章 系统硬件设计。详细介绍主控最小系统及各外围功能模块的硬件电路设计与工作原理，给出 MCU 引脚资源分配表。

第 4 章 下位机软件设计。介绍下位机软件总体架构与开发环境，详细阐述主程序、时间基准、各驱动模块、控制算法与串口协议的软件实现。

第 5 章 蓝牙通信与上位机软件设计。介绍 ESP32-S3 蓝牙接收端的 GATT 服务设计，以及 Web Serial 上位机与 Node.js 后端、MySQL 数据库的设计与实现。

第 6 章 系统调试与测试。介绍测试环境与方法，对系统各模块及整机进行测试，分析测试结果。

第 7 章 总结与展望。总结全文工作，指出存在的不足，并对后续改进方向进行展望。

---

# 第 2 章 系统需求分析与总体方案设计

## 2.1 系统需求分析

需求分析是系统设计的起点。本节从功能性需求和非功能性需求两个维度，明确系统应当具备的能力与约束。

### 2.1.1 功能性需求

通过对宠物烘干这一应用场景的分析，结合传统设备的不足，确定系统应满足以下功能性需求：

（1）**环境参数采集需求**。系统应能够实时采集烘干箱内的温度（和湿度）数据。考虑到箱体内温度分布可能不均匀，采用双点采集以提高代表性与可靠性，并为冗余容错提供基础。

（2）**温度控制与保护需求**。系统应能根据当前温度与设定的报警温度阈值，自动控制加热回路的通断，将箱内温度维持在安全合理的范围内；当温度显著超过阈值时，应触发声光报警，防止烫伤。

（3）**风速调节需求**。系统应支持对烘干风机进行无级（0~100%）调速，以适应不同体型、不同毛量宠物的烘干需求，兼顾烘干效率与宠物舒适度。

（4）**定时烘干需求**。系统应支持设定烘干时长并倒计时，时间到后自动停机，避免过度烘干；同时支持在烘干过程中手动增减时间。

（5）**预设模式需求**。为简化操作，系统应预置面向"宠物种类—体型"的多种烘干模式（小型犬、中型犬、大型犬、小型猫、中型猫、大型猫），一键启动即可自动配置烘干时长、报警温度和风机转速。

（6）**宠物安全监测需求**。系统应能检测宠物是否在箱体内。在监测使能且烘干运行的状态下，若宠物离开超过设定时间（如 5 秒），应立即停机并报警，保障宠物安全。

（7）**人机交互需求**。系统应通过 OLED 屏幕实时显示双路温度、剩余时间、报警温度、继电器状态等信息；通过物理按键支持本地参数调节；通过蜂鸣器与指示灯提供声光提示。

（8）**远程控制需求**。系统应支持通过手机蓝牙下发控制指令（如选择模式），实现远程操作。

（9）**数据可视化与持久化需求**。系统应支持上位机实时显示运行数据，并将温湿度、倒计时、报警温度等关键数据存入数据库，形成可查询、可追溯的历史记录。

### 2.1.2 非功能性需求

（1）**实时性**。温度采集与控制、按键响应、报警触发等应具有良好的实时性，控制周期满足烘干过程的动态要求。

（2）**可靠性与安全性**。系统应具备过温保护、离位保护等多重安全机制；通信应有容错处理；长时间运行应稳定不死机。

（3）**易用性**。本地操作应简单直观，远程操作应便捷；预设模式降低了用户的使用门槛。

（4）**低成本**。在满足功能的前提下，尽量选用性价比高、易于获取的元器件，控制整机成本。

（5）**可扩展性**。系统架构应具有良好的模块化与可扩展性，便于后续增加传感器种类、控制策略或云端服务。

## 2.2 系统总体架构

根据需求分析，本系统采用分层、分模块的设计思想，整体可划分为**感知执行层、控制层、通信桥接层、应用与数据层**四个层次，总体架构如图 2-1 所示（文字描述）。

```
        ┌─────────────────────────────────────────────────────┐
        │                应用与数据层（上位机）                  │
        │  浏览器(Web Serial) ── Node.js 本地服务 ── MySQL 数据库 │
        └───────▲──────────────────────────────▲───────────────┘
                │ USB 串口(STM32)                │ USB 串口(ESP32)
        ┌───────┴───────┐               ┌────────┴────────┐
        │   控制层        │   USART1      │  通信桥接层      │   BLE
        │ STM32F103C8T6  │◀────────────▶│   ESP32-S3      │◀──────  手机 App
        └───────┬────────┘  指令/遥测     │ (NimBLE GATT)   │
                │                         └─────────────────┘
   ┌────────────┼──────────────────────────────────────────┐
   │            ▼  感知执行层                                  │
   │ 双DHT11  在位检测  按键 │ OLED  PWM风机  继电器  蜂鸣器 LED │
   │     （传感器/输入）      │        （执行/输出）             │
   └──────────────────────────────────────────────────────┘
```

**图 2-1 系统总体架构示意图**

各层功能说明如下：

- **感知执行层**：包括两路 DHT11 温湿度传感器、宠物在位检测传感器、四个独立按键等输入设备，以及 OLED 显示屏、PWM 调速风机、继电器（加热回路）、蜂鸣器、LED 指示灯等输出/执行设备。该层直接面向物理世界，完成数据采集与动作执行。

- **控制层**：以 STM32F103C8T6 为核心，负责采集传感器数据、运行控制算法（温度闭环、倒计时、模式管理、安全监测）、驱动执行机构、刷新显示，并通过 USART1 与外部进行命令交互和遥测数据输出。它是整个系统的"大脑"。

- **通信桥接层**：以 ESP32-S3 为核心，运行 BLE GATT 服务器，接收手机 App 通过蓝牙写入的指令，并通过其 USB 串口将指令透传给上位机；上位机再将指令经 STM32 的串口转发给下位机。该层实现了"移动端—有线主机"的无线桥接。

- **应用与数据层**：即上位机，由运行在浏览器中的网页（基于 Web Serial API）和本地 Node.js 服务组成。网页负责连接 STM32 与 ESP32 两路串口、实时显示与解析遥测数据、下发指令；Node.js 服务负责提供网页、管理数据库配置，并将解析后的遥测数据写入 MySQL 数据库。

值得说明的是，系统的数据流向是双向的：

- **下行（控制）数据流**：手机 App → BLE → ESP32-S3 → USB 串口 → 上位机网页 → STM32 串口 → STM32 执行；或者用户直接在上位机网页/STM32 本地按键发出指令。

- **上行（遥测）数据流**：STM32 周期性地通过 USART1 输出运行状态文本 → 上位机网页解析并显示 → Node.js 服务写入 MySQL。

这种架构既保留了下位机本地独立运行的能力（不依赖网络也能完成基本烘干），又通过蓝牙和上位机扩展了远程控制与数据管理能力，体现了"边缘自治、云端增强"的物联网设计理念。

## 2.3 关键技术选型与论证

### 2.3.1 主控芯片选型

主控芯片是整个下位机的核心。常见的候选方案有 51 单片机、STM32 系列、ESP32 系列等。

- **51 单片机**：成本最低，但主频低、片上外设少、定时器与通信资源有限，难以同时高效驱动双传感器、PWM、OLED 与多任务调度。
- **ESP32 系列**：自带 Wi-Fi/蓝牙，性能强，但对于本系统的实时控制任务而言资源略显冗余，且其作为主控时蓝牙协议栈会占用较多资源；本系统将其专门用作蓝牙桥更为合理。
- **STM32F103C8T6**：基于 ARM Cortex-M3 内核，主频 72 MHz，拥有 64 KB Flash、20 KB SRAM，集成多路定时器（含高级定时器）、多个 USART、I²C、SPI、ADC 等丰富外设，开发资料丰富、生态成熟、价格低廉（俗称"最小系统板"），完全能够满足本系统对实时控制、PWM 调速、多模块驱动的需求。

综合性能、成本与开发便利性，本系统主控选用 **STM32F103C8T6**。

### 2.3.2 温湿度传感器选型

温度采集是温控的基础。候选方案包括模拟温度传感器（如 LM35、热敏电阻 NTC）、数字单总线传感器（DHT11/DHT22、DS18B20）等。

- LM35、NTC 等模拟方案需占用 ADC 通道并进行标定，且只能测温、不能测湿。
- DS18B20 精度高，但仅测温。
- **DHT11** 是一款集成了温度与湿度测量的数字传感器，采用单总线通信，外围电路简单、成本低廉、使用方便，温度测量范围 0~50 ℃、湿度 20%~90% RH，分辨率 1 ℃ / 1% RH，完全满足宠物烘干场景（温度通常控制在 50 ℃ 以下）的需求。

因此选用 **DHT11**，并采用双路部署，分别监测箱内不同位置的温度，提高测量的代表性和系统的容错能力。

### 2.3.3 无线通信方案选型

远程控制的无线方案主要有 Wi-Fi、蓝牙、ZigBee 等。

- Wi-Fi 传输速率高、可直连云端，但功耗较高、配网相对复杂。
- ZigBee 适合大规模组网，但需要额外网关，消费级单机产品中应用较少。
- **蓝牙低功耗（BLE）** 功耗低、成本低，且智能手机原生支持，用户无需额外硬件即可通过 App 直接连接控制，非常适合"手机—单机设备"的近距离控制场景。

本系统选用 **ESP32-S3** 作为蓝牙模块，并基于 **NimBLE** 协议栈实现。相比 Bluedroid，NimBLE 内存占用更小、更适合纯 BLE 应用。ESP32-S3 通过 USB 串口与上位机相连，充当"BLE 到串口"的透明桥接。

### 2.3.4 上位机方案选型

上位机负责数据可视化与存储。传统方案多用 C#/Qt 等开发桌面客户端，需针对不同操作系统编译分发。本系统创新性地采用 **Web Serial API + Node.js** 方案：

- **Web Serial API** 是现代 Chromium 内核浏览器（Chrome/Edge）提供的接口，允许网页在用户授权后直接访问本地串口设备，免安装、跨平台。
- **Node.js** 提供轻量本地 HTTP 服务，既托管网页，又承担与 MySQL 数据库交互的职责（浏览器不直接连数据库，由本地服务代理写库）。

该方案部署简单、维护方便、界面美观，且天然支持多串口（同时连接 STM32 与 ESP32），契合本系统需求。

### 2.3.5 数据库选型

数据持久化选用 **MySQL** 关系型数据库。MySQL 开源免费、稳定可靠、生态成熟，适合存储结构化的遥测日志。系统通过调用本地 `mysql` 命令行客户端执行 SQL 完成建库、建表与数据插入，避免在浏览器端引入数据库驱动，保证了安全性（数据库凭据仅保存在本地服务端）。

## 2.4 系统工作流程

系统上电后的整体工作流程如下：

（1）**初始化阶段**。STM32 完成系统时钟、延时、LED、串口、OLED、蜂鸣器、PWM、继电器、按键、在位检测、时间基准定时器等的初始化，并对两路 DHT11 进行初始化自检，自检通过后在 OLED 上绘制静态界面框架，进入主循环。ESP32-S3 完成 NVS、NimBLE 协议栈初始化，注册 GATT 服务并开始广播。上位机网页加载后读取数据库配置、枚举已授权串口，等待用户连接。

（2）**待机/运行阶段**。STM32 默认进入运行态（初始倒计时 60 秒）。主循环中周期性地：扫描串口命令与本地按键、读取在位检测、按时间基准推进倒计时、每秒读取一次双路 DHT11、根据温度执行闭环控制与报警、刷新 OLED 显示、通过串口输出遥测文本。

（3）**模式启动**。用户可通过本地按键调节参数，或通过上位机/手机蓝牙下发模式指令（如 `dog_medium`）。收到模式指令后，STM32 按预设值重置倒计时、报警温度、风机转速，并复位安全监测状态，开始烘干。

（4）**烘干过程控制**。烘干运行期间，系统根据双路温度与报警温度执行加热回路通断控制；温度超限时蜂鸣器报警；若使能在位监测且宠物离箱超时，立即停机报警。倒计时归零后自动停机。

（5）**数据上报与存储**。STM32 每秒输出一行遥测文本，上位机解析后实时显示，并（在启用数据库自动入库时）将数据写入 MySQL，形成历史记录。

后续各章将分别从硬件、下位机软件、蓝牙与上位机软件三个方面，对上述流程涉及的具体设计展开详细论述。

---

# 第 3 章 系统硬件设计

本章对系统下位机及蓝牙模块的硬件电路进行详细设计。整个硬件系统以 STM32F103C8T6 最小系统为核心，外围扩展温湿度采集、显示、风机驱动、加热控制、报警、输入与在位检测等模块，并通过 ESP32-S3 模组实现蓝牙桥接。下面分模块阐述。

## 3.1 主控最小系统

### 3.1.1 STM32F103C8T6 简介

STM32F103C8T6 是意法半导体（ST）推出的一款基于 ARM Cortex-M3 内核的 32 位微控制器，是 STM32F1 系列中应用极为广泛的型号之一。其主要特性如下：

- 内核：ARM Cortex-M3，最高主频 72 MHz；
- 存储器：64 KB Flash 程序存储器，20 KB SRAM；
- 封装：LQFP48，共 37 个可用 GPIO；
- 定时器：3 个通用定时器（TIM2~TIM4）、1 个高级定时器（TIM1），均支持 PWM 输出；
- 通信接口：2 个 USART、2 个 SPI、2 个 I²C、1 个 USB、1 个 CAN；
- 模数转换：2 个 12 位 ADC，多达 10 个外部通道；
- 工作电压：2.0~3.6 V，典型 3.3 V。

该芯片以"最小系统板"（俗称"蓝色药丸 Blue Pill"）的形式被广泛使用，资料丰富、价格低廉，非常适合本课题的开发。

### 3.1.2 最小系统电路

STM32 最小系统是保证 MCU 正常工作的最小电路集合，主要包括：

（1）**电源电路**：3.3 V 供电，每个电源引脚就近配置 0.1 μF 去耦电容，VDDA 通过磁珠或电感与 VDD 隔离并加滤波电容，以降低 ADC 参考噪声。

（2）**时钟电路**：外接 8 MHz 无源晶振作为高速外部时钟（HSE），经内部 PLL 9 倍频得到 72 MHz 系统时钟；外接 32.768 kHz 晶振用于 RTC（本系统可选）。两组晶振均配置合适的负载电容。

（3）**复位电路**：NRST 引脚接 10 kΩ 上拉电阻与 0.1 μF 电容组成的上电复位电路，并引出复位按键，便于手动复位。

（4）**启动配置**：BOOT0、BOOT1 通过跳线/电阻配置启动模式，正常运行时 BOOT0 接地，从内部 Flash 启动。

（5）**调试下载接口**：引出 SWD 接口（SWDIO、SWCLK），配合 ST-Link 仿真器完成程序下载与在线调试。

在软件层面，系统时钟通过 `SystemInit()` 与 `system_stm32f10x.c` 完成配置，最终 SYSCLK = 72 MHz，AHB = 72 MHz，APB1 = 36 MHz，APB2 = 72 MHz，为各定时器与外设提供时钟基准。

## 3.2 温湿度采集模块

### 3.2.1 DHT11 工作原理

DHT11 是一款含有已校准数字信号输出的温湿度复合传感器，内部包含一个电阻式感湿元件和一个 NTC 测温元件，并与一颗 8 位单片机相连。它通过单总线（single-bus）与主控通信，每次通信传输 40 位数据，依次为：湿度整数、湿度小数、温度整数、温度小数、校验和。其中校验和应等于前四字节之和的低 8 位，用于检验数据传输的正确性。DHT11 的温度测量范围为 0~50 ℃（精度 ±2 ℃），湿度范围 20%~90% RH（精度 ±5% RH），完全满足宠物烘干箱的监测需求。

### 3.2.2 双路 DHT11 硬件设计

为提高箱内温度测量的代表性，本系统部署两路 DHT11，分别安装于箱体内不同位置（如进风口附近与宠物所处区域）。两路传感器的数据引脚分别连接到 STM32 的 **PA11（DHT11_1）** 与 **PA12（DHT11_2）**。

DHT11 的 DATA 引脚为开漏/准双向结构，需外接一个 4.7 kΩ~10 kΩ 的上拉电阻到 VCC，以保证总线空闲时为高电平。电源端就近并联 0.1 μF 去耦电容以滤除供电噪声。由于单总线在不同时刻需要在"输出"（主机发起始信号）与"输入"（读取传感器应答与数据）之间切换，因此在软件中将对应 GPIO 在推挽输出与浮空/上拉输入两种模式间动态配置。

双路设计还带来了**冗余容错**的好处：当某一路传感器读取出错（校验失败）时，系统仍可依据另一路数据进行控制与显示，提高了系统的鲁棒性。

## 3.3 OLED 显示模块

系统采用一块 0.96 寸、分辨率 128×64 的 OLED 显示屏，驱动芯片为 SSD1306。OLED 具有自发光、对比度高、视角广、功耗低等优点，在小型嵌入式人机界面中应用广泛。

本设计采用 **软件模拟 I²C** 方式驱动 OLED，时钟线 SCL 与数据线 SDA 分别连接 STM32 的 **PB8（SCL）** 与 **PB9（SDA）**。相比硬件 I²C，软件 I²C 不占用专用外设、引脚选择灵活，时序完全由软件控制，便于移植。OLED 的 SCL、SDA 两条信号线需各接一个上拉电阻（部分模块已内置）。供电为 3.3 V，并配置去耦电容。

OLED 用于显示双路温度、剩余烘干时间、当前报警温度以及继电器状态等关键运行信息，使用户在本地即可直观掌握设备状态。

## 3.4 PWM 风机驱动模块

### 3.4.1 调速原理

烘干风机采用直流风机，其转速与平均供电电压近似成正比。本系统采用 **PWM（脉宽调制）** 方式实现风机无级调速：通过改变 PWM 信号的占空比，即可改变施加在风机上的平均电压，从而连续调节风速。占空比越大，平均电压越高，风速越快。

为避免人耳可闻的电磁噪声，PWM 频率选取在 20 kHz 以上。本系统利用 STM32 的 **TIM3 通道 1** 产生 PWM，输出引脚为 **PA6**。定时器配置为：预分频系数 PSC = 0（即 72 MHz 计数时钟），自动重装载值 ARR = 2880−1，则 PWM 频率为 72 MHz / 2880 ≈ **25 kHz**，处于超声频段，工作时安静无啸叫。占空比通过比较寄存器 CCR1 设置，CCR1 = ARR × 占空比，可实现 0~100% 的连续调节。

### 3.4.2 驱动电路

STM32 的 GPIO 输出电流有限（最大约 25 mA），无法直接驱动风机。PWM 信号经由 MOS 管（如 N 沟道 MOSFET）或专用电机驱动芯片进行功率放大后驱动风机。典型电路中，PWM 信号接 MOS 管栅极，风机接在漏极与电源之间，源极接地；MOS 管栅极配置下拉电阻保证上电默认关断，并在风机两端反并联续流二极管以吸收感性负载关断时的反向电动势，保护开关器件。风机电源根据其额定电压单独供给（如 12 V），与 3.3 V 控制电源共地。

## 3.5 继电器加热控制模块

加热回路（如 PTC 加热片或加热丝）由继电器控制通断。继电器是一种电气隔离的开关器件，可用低压控制信号驱动大功率负载，安全可靠。

本系统继电器控制信号由 STM32 的 **PB11** 引脚输出。由于 GPIO 无法直接提供继电器线圈所需的驱动电流，控制信号经三极管（如 NPN 管 S8050）或 ULN2003 等驱动后控制继电器线圈，线圈两端反并联续流二极管以保护驱动管。继电器常开/常闭触点串接在加热回路中，实现加热的通断。

本设计中继电器控制采用**低电平有效**逻辑：引脚输出低电平时继电器动作（吸合），高电平时释放。软件中通过对 GPIO 的 BSRR/BRR 寄存器操作实现快速置位/复位，并提供状态查询宏 `Relay_GetStatus()`。继电器在系统中承担两重角色：一是配合温度闭环控制，在温度超过报警阈值时切断加热回路实现过温保护；二是在烘干结束或触发安全停机时切换到相应状态，配合声光提示。

## 3.6 蜂鸣器与指示灯模块

### 3.6.1 蜂鸣器报警

系统采用有源蜂鸣器实现声音报警。有源蜂鸣器内部自带振荡源，只需施加直流电平即可发声，驱动简单。蜂鸣器控制引脚为 STM32 的 **PB13**，经三极管放大后驱动蜂鸣器（蜂鸣器电流通常为数十毫安，超过 GPIO 直接驱动能力）。软件中提供开、关、翻转、定时鸣叫等接口。蜂鸣器在以下情形触发报警：箱内温度超过报警温度阈值一定范围（本系统为报警温度 +5 ℃）时持续鸣叫；宠物离位超时触发安全停机时鸣叫。

### 3.6.2 LED 状态指示灯

系统利用 STM32 最小系统板载的 LED（连接 **PC13**，低电平点亮）作为运行状态指示灯。在主循环中每完成一次数据采集即翻转一次 LED，形成"心跳"闪烁，用于直观判断程序是否正常运行（若 LED 停止闪烁，则说明程序可能卡死）。

## 3.7 按键输入模块

为支持本地参数调节，系统设置 4 个独立轻触按键，分别连接 STM32 的 **PA0、PA1、PA2、PA3**。引脚配置为上拉输入，按键另一端接地，按下时引脚被拉低。四个按键的功能定义如下：

| 按键 | 引脚 | 功能 |
| --- | --- | --- |
| K1（UP） | PA0 | 烘干倒计时 +60 秒 |
| K2（DOWN） | PA1 | 烘干倒计时 −60 秒 |
| K3（TEMP_UP） | PA2 | 报警温度 +1 ℃ |
| K4（TEMP_DOWN） | PA3 | 报警温度 −1 ℃ |

**表 3-1 按键功能定义**

按键在按下/释放瞬间存在机械抖动，硬件上可并联小电容滤波，软件上通过延时消抖与状态标志位配合，确保一次按下只触发一次有效动作（详见第 4 章）。

## 3.8 宠物在位检测模块

宠物在位检测是本系统的重要安全特性。检测传感器可采用红外对射/反射式传感器或称重（压力）传感器，输出一路数字电平信号，接入 STM32 的 **PB12** 引脚（配置为上拉输入）。约定：当宠物在箱内时传感器使引脚为低电平（检测到遮挡/重量），离开时为高电平。软件中通过 `PetSensor_IsPresent()` 读取并取反，得到"是否在位"的布尔结果。

在烘干运行且监测使能（`pet_on` 指令开启）的情况下，系统每 100 ms 采样一次在位状态。一旦检测到宠物持续离开超过 5 秒（`PET_SENSOR_ABSENCE_TIMEOUT_MS = 5000`），立即执行安全保护：关闭风机（PWM 占空比置 0）、切换继电器、开启蜂鸣器报警，并停止烘干计时。该机制可有效防止宠物逃脱后设备空转或潜在的安全风险。

## 3.9 ESP32-S3 蓝牙模块

蓝牙通信由独立的 ESP32-S3 模组承担。ESP32-S3 是 Espressif 推出的高性能 SoC，集成双核 Xtensa LX7 处理器、Wi-Fi 与 Bluetooth 5（LE），并具备丰富的外设与较大的 RAM/Flash，足以流畅运行 BLE 协议栈。

在本系统中，ESP32-S3 不直接连接烘干箱的执行机构，而是作为**"蓝牙—串口"桥接器**：

- 通过 BLE 与手机 App 建立连接，接收 App 写入的控制指令；
- 通过其内置 USB（CDC）或 UART 与上位机电脑相连，将收到的指令以文本形式打印输出。

上位机网页同时连接 STM32 与 ESP32 两个串口，当 ESP32 串口收到指令文本时，网页自动将其转发到 STM32 串口，从而实现"手机→蓝牙→ESP32→上位机→STM32"的远程控制链路。ESP32-S3 模组独立 3.3 V 供电，其 USB 口同时提供供电与数据通道，硬件连接简单。蓝牙固件基于 ESP-IDF 与 NimBLE 实现，具体软件设计见第 5 章。

## 3.10 电源模块

系统涉及多种电压等级：STM32、OLED、传感器等数字部分需要 3.3 V；风机、加热片、继电器线圈等功率部分通常需要 5 V 或 12 V。电源设计要点如下：

（1）由外部适配器提供主电源（如 12 V/2 A），经 DC-DC 降压模块得到 5 V，再由 LDO（如 AMS1117-3.3）稳压得到 3.3 V 供数字部分使用；
（2）功率地与信号地采用单点共地，并适当加粗功率回路走线，减小地弹与干扰；
（3）各芯片电源引脚就近配置去耦电容；继电器、风机等感性/大电流负载与控制电路在供电上尽量分区，必要时加磁珠隔离，以保证 MCU 与传感器工作稳定。

## 3.11 硬件资源分配

综合上述各模块，STM32F103C8T6 的引脚资源分配如表 3-2 所示。

| 功能模块 | 信号 | 引脚 | 配置 |
| --- | --- | --- | --- |
| 温湿度传感器 1 | DHT11_1 DATA | PA11 | 推挽输出/浮空输入切换 |
| 温湿度传感器 2 | DHT11_2 DATA | PA12 | 推挽输出/浮空输入切换 |
| 串口通信 | USART1_TX | PA9 | 复用推挽输出 |
| 串口通信 | USART1_RX | PA10 | 浮空输入 |
| 风机调速 | TIM3_CH1 PWM | PA6 | 复用推挽输出 |
| 按键 K1 | KEY_UP | PA0 | 上拉输入 |
| 按键 K2 | KEY_DOWN | PA1 | 上拉输入 |
| 按键 K3 | KEY_TEMP_UP | PA2 | 上拉输入 |
| 按键 K4 | KEY_TEMP_DOWN | PA3 | 上拉输入 |
| OLED 显示 | I²C_SCL | PB8 | 推挽输出（软件 I²C） |
| OLED 显示 | I²C_SDA | PB9 | 推挽输出（软件 I²C） |
| 继电器 | RELAY_CTRL | PB11 | 推挽输出（低有效） |
| 宠物在位检测 | PET_SENSOR | PB12 | 上拉输入 |
| 蜂鸣器 | BUZZER | PB13 | 推挽输出 |
| 状态指示灯 | LED | PC13 | 推挽输出（低有效） |
| 时间基准 | TIM2 | — | 内部定时，10 kHz 计数 |

**表 3-2 STM32 引脚资源分配表**

由表可见，本设计合理利用了 GPIOA、GPIOB、GPIOC 三个端口的引脚，并将定时器资源中的 TIM3 用于 PWM 输出、TIM2 用于软件时间基准，外设分配清晰、互不冲突，为软件设计奠定了基础。

---

# 第 4 章 下位机软件设计

## 4.1 软件总体架构与开发环境

### 4.1.1 开发环境

下位机软件基于 STM32 标准外设库（StdPeriph Library，ST3.x）进行开发，开发工具为 Keil MDK-ARM（μVision5），编译器为 ARMCC。程序通过 ST-Link 仿真器经 SWD 接口下载到目标板，并可进行在线调试。串口调试方面，使用 USART1（115200/8-N-1）输出运行日志，借助串口助手或本系统自带的 Web 上位机进行观测。

### 4.1.2 软件分层结构

下位机软件采用分层、模块化的组织方式，自底向上可分为三层：

（1）**硬件抽象/驱动层**：封装各外设的底层驱动，包括 delay（微秒/毫秒延时）、usart（串口收发与命令解析）、led、dht11、oled、buzzer、pwm、relay、key 等模块。每个模块对外提供简洁的功能接口（如 PWM_SetDuty()、DHT11_Read_Data()），隐藏寄存器操作细节。

（2）**功能逻辑层**：在 main.c 中实现，包括烘干模式管理、倒计时、温度闭环控制、宠物离位监测、按键/串口动作映射等业务逻辑。该层调用驱动层接口完成具体功能。

（3）**调度层**：以主循环（超级循环 super-loop）配合 TIM2 软件时间基准实现多个周期性任务的"伪并发"调度。

这种分层设计使得各模块高内聚、低耦合，便于独立开发、测试与复用。整体软件目录结构对应工程中的 USER（主程序）、SYSTEM（delay/sys/usart）、HARDWARE（各外设驱动）等文件夹。

## 4.2 主程序设计

主程序 main() 的执行流程分为初始化与主循环两部分，流程如图 4-1 所示（文字描述）。

```
   上电复位
      │
      ▼
  SystemInit() 系统时钟配置(72MHz)
      │
      ▼
  delay_init / LED / USART1 / OLED / Buzzer
  PWM / Relay / Key / PetSensor / Timebase 初始化
      │
      ▼
  DHT11_1、DHT11_2 初始化自检（失败则循环重试）
      │
      ▼
  绘制 OLED 静态界面框架
      │
      ▼
 ┌──────────── 主循环 while(1) ───────────┐
 │ 1. 读取串口命令并执行                    │
 │ 2. 扫描本地按键并执行                    │
 │ 3. 读取 TIM2 计数，累加经过的毫秒         │
 │ 4. 每 1ms：推进 DHT读取/在位/倒计时 计时   │
 │ 5. 每 1s：读取双路DHT11、温度控制、报警    │
 │ 6. 刷新 OLED 显示                        │
 │ 7. delay_ms(10)                         │
 └─────────────────────────────────────────┘
```

**图 4-1 主程序流程图**

初始化阶段依次完成系统时钟、延时、各外设的初始化，并对两路 DHT11 进行自检，自检失败会打印错误并循环重试，确保传感器就绪后再进入主逻辑。随后在 OLED 上绘制温度、时间、报警温度、继电器状态等标签的静态部分。主循环中，程序以非阻塞的方式轮询串口命令与按键、依据时间基准推进各计时器、定时读取传感器并执行控制、刷新显示。循环末尾的 delay_ms(10) 既限制了循环频率，也作为按键扫描的基础节拍。

主程序中维护的关键状态变量包括：倒计时剩余秒数 countdown、运行标志 running、报警温度 alarm_temp、风机占空比 fan_pwm_duty、在位监测使能标志 pet_monitor_enabled、双路温湿度 temp1/humi1/temp2/humi2 等。这些变量在各任务间共享，构成系统的运行上下文。

## 4.3 软件时间基准与任务调度

### 4.3.1 设计思路

本系统需要同时处理多个不同周期的任务：DHT11 每秒读取一次、在位检测每 100 ms 采样一次、倒计时每秒递减一次。若简单使用阻塞延时，将导致各任务相互拖延、实时性差。为此，本系统设计了一个基于硬件定时器的**软件时间基准**，将"时间的流逝"与"任务的执行"解耦，实现轻量级的协作式调度。

### 4.3.2 TIM2 时间基准

系统将 TIM2 配置为自由运行的计数器，作为统一的时间基准。预分频系数取 7200−1，使计数频率为 72 MHz / 7200 = 10 kHz，即每个计数（tick）对应 0.1 ms，自动重装载值取最大的 0xFFFF，计数器持续自由运行。

在主循环中，程序读取当前计数值并与上次读数相减，得到本轮循环经过的 tick 数（利用 16 位无符号回绕特性自动处理溢出），再将其累加到 tick_100us_acc。随后以 1 ms（10 个 tick）为单位"消费"累加值，在每个 1 ms 内推进各任务的毫秒计时器 dht_elapsed_ms、pet_sample_elapsed_ms、countdown_elapsed_ms。当某个计时器累加到设定阈值时，置位对应的执行标志或直接执行动作。例如 dht_elapsed_ms 达到 1000 时置位 dht_read_flag，触发一次 DHT11 读取；pet_sample_elapsed_ms 达到 100 时执行一次在位采样；countdown_elapsed_ms 达到 1000 时倒计时减一。

这种"时间基准 + 累加消费"的方式具有以下优点：①各任务周期独立、互不阻塞；②即使主循环因 DHT11 读取等耗时操作偶有抖动，也不会丢失时间（累加机制保证时间守恒）；③实现简单，无需引入实时操作系统，资源开销极小。

## 4.4 DHT11 单总线驱动设计

### 4.4.1 单总线时序

DHT11 采用单总线协议，一次完整的数据读取过程如下：

（1）**起始信号**：主机将总线拉低至少 18 ms，再拉高 20~40 μs，然后释放总线（切换为输入）。

（2）**传感器响应**：DHT11 检测到起始信号后，先拉低总线 80 μs 作为响应，再拉高 80 μs 表示准备发送数据。

（3）**数据传输**：DHT11 依次发送 40 位数据。每一位以 50 μs 低电平开始，随后的高电平持续时间决定该位的值：高电平约 26~28 μs 表示"0"，约 70 μs 表示"1"。

（4）**校验**：40 位数据分为 5 字节，最后一字节为前四字节之和的低 8 位校验码。

### 4.4.2 驱动实现

驱动以 sensor 参数（1 或 2）区分两路传感器，主要函数包括：

- DHT11_Mode(sensor, mode)：动态切换 GPIO 为输出（推挽）或输入（浮空）模式，以适应主机发送起始信号与读取应答/数据的需要。
- DHT11_Rst(sensor)：产生起始信号（拉低约 20 ms 后拉高约 13 μs）。
- DHT11_Check(sensor)：检测传感器的 80 μs 低/高响应，带超时保护（重试计数达 100 即判失败返回 1）。
- DHT11_Read_Bit/Byte(sensor)：通过判断高电平持续时间读取每一位/字节。读取一位时先等待低电平结束，再延时 40 μs 后采样电平，高则为 1，低则为 0。
- DHT11_Read_Data(sensor, temp, humi)：完成一次完整读取，校验通过后输出温度与湿度，校验失败返回 1。

驱动中的校验逻辑将前四字节求和取低 8 位与第五字节比较，相等才认为数据有效，再分别取出湿度（buf[0]）与温度（buf[2]）。所有等待环节均设置了超时重试上限，避免传感器异常时程序陷入死循环，保证了主循环的健壮性。主程序中每秒触发一次双路读取，读取失败时打印错误信息但不影响其余功能继续运行。

## 4.5 PWM 调速与风机控制

PWM 模块直接操作 TIM3 寄存器实现。初始化时配置 PA6 为复用推挽输出、TIM3 通道 1 为 PWM1 模式并使能预装载：预分频 PSC=0、自动重装载 ARR=2880−1，得到约 25 kHz 的 PWM 频率；通过比较寄存器 CCR1 控制占空比，CCR1 = 2880 × 占空比%。

占空比设置与读取以百分比为接口，便于上层调用：PWM_SetDuty(duty) 将 0~100 的占空比换算为 CCR1 值并写入；PWM_GetDuty() 反算当前占空比百分比。此外，模块还提供了 PWM_SetLevel() 档位接口（OFF/低/中/高分别对应 0%/30%/60%/90%），便于按档位粗调。

在主程序中，风机占空比既可由预设模式自动设定，也可通过串口 pwm=N 命令手动设定（0~100）。手动设定时，若系统处于运行态则立即生效，否则保存为下次运行时的目标值，实现灵活的风速控制。25 kHz 的工作频率位于超声频段，风机驱动时安静无可闻啸叫。

## 4.6 继电器温度闭环控制

继电器模块直接操作 GPIOB 寄存器，采用低电平有效逻辑。Relay_On() 通过 BRR 将引脚置低使继电器吸合，Relay_Off() 通过 BSRR 置高使其释放，并提供状态查询宏。

温度闭环控制在每秒一次的 DHT11 读取之后执行，核心为一种双阈值（bang-bang）控制策略，以两路温度中的较高者为准（任一路超限即动作）：当箱内温度超过设定的报警温度时，继电器动作切断加热回路，使温度回落；当温度回落到报警温度以下时，继电器复位恢复加热；当温度进一步超过报警温度 5 ℃ 以上时，触发蜂鸣器声光报警，提示用户注意；温度回落后自动解除报警。

这种两级阈值（报警温度、报警温度+5 ℃）的设计在保证安全的同时，避免了继电器在单一阈值附近频繁动作，兼顾了控温的稳定性与过温保护的可靠性，非常适合对控温精度要求不高、但对安全上限要求严格的宠物烘干场景。需要强调的是，温度控制仅在烘干运行（running=1）时生效，停机后不再动作执行机构，符合直觉且节能。

## 4.7 烘干模式与倒计时管理

### 4.7.1 预设模式参数

系统针对宠物的"种类×体型"组合预置了 6 种烘干模式，每种模式封装了烘干时长、报警温度、风机占空比三个参数，如表 4-1 所示。

| 模式 | 命令 | 烘干时长 | 报警温度 | 风机占空比 |
| --- | --- | --- | --- | --- |
| 小型犬 | dog_small | 1200 s（20 min） | 34 ℃ | 60% |
| 中型犬 | dog_medium / dog | 1500 s（25 min） | 36 ℃ | 80% |
| 大型犬 | dog_large | 1800 s（30 min） | 38 ℃ | 100% |
| 小型猫 | cat_small | 1000 s（≈16.7 min） | 32 ℃ | 50% |
| 中型猫 | cat_medium / cat | 1200 s（20 min） | 34 ℃ | 70% |
| 大型猫 | cat_large | 1400 s（≈23 min） | 36 ℃ | 90% |

**表 4-1 预设烘干模式参数表**

参数设计遵循"体型越大、毛量越多，所需时长越长、可承受温度与风速越高"的原则；同时考虑到猫对温度更敏感，猫的各项参数整体低于同体型的犬，体现了对不同宠物习性的差异化考量。

### 4.7.2 模式启动逻辑

收到模式命令时，调用统一的 StartDryMode() 函数完成参数装载与状态复位。该函数设置倒计时与报警温度、立即将风机调到设定转速、关闭蜂鸣器与继电器（进入正常烘干态）、置运行标志，并清零倒计时毫秒计时器，最后通过串口回显模式信息（模式名、时长、报警温度、占空比）便于调试与上位机记录。每次启动模式时还会调用 ResetPetAbsenceState() 复位宠物离位监测状态，避免历史状态干扰本次烘干。将参数装载逻辑抽取为单一函数，使 6 种模式的处理高度复用，代码简洁、易于维护与扩展。

### 4.7.3 倒计时与自动停机

倒计时由时间基准每秒驱动递减。当倒计时归零时，系统自动停机：清运行标志、关闭风机（PWM 占空比置 0）、动作继电器、关闭蜂鸣器，并打印停机日志。

此外，用户可在烘干过程中通过按键或串口命令对倒计时进行 ±60 s 的调整、对报警温度进行 ±1 ℃ 的微调。调整逻辑由 ApplyKeyAction() 统一处理：倒计时减少时设有下限保护（不小于 60 s），报警温度调整时限制在 20~50 ℃ 范围内，防止误设到危险值。本地按键与串口命令通过 USART1_CommandToKey() 映射到同一套动作处理函数，保证了两种输入方式行为一致。

## 4.8 宠物离位安全监测

宠物离位监测是系统的核心安全功能，在时间基准的 100 ms 采样节拍中执行，逻辑要点如下：仅在监测使能（pet_monitor_enabled）且烘干运行（running）时生效；每 100 ms 读取一次在位传感器，只要检测到宠物在位就清零离位计时 pet_absence_elapsed_ms；若持续离位，则每个采样周期将离位计时累加 100 ms；当离位计时达到 5000 ms 阈值且尚未触发过报警（pet_absence_alarm_triggered=0）时，执行一次性的安全停机：清运行标志、关风机、动作继电器、开启蜂鸣器报警，并通过串口打印告警。

通过 pet_absence_alarm_triggered 标志位保证安全停机动作只执行一次，避免重复触发；监测功能由串口 pet_on/pet_off 命令在线开关，灵活适应不同使用场景（如调试时可关闭）。该机制可有效防止宠物在烘干中途逃脱后设备空转，杜绝潜在的安全隐患与能源浪费，是本系统区别于普通烘干箱的重要安全特性。

## 4.9 按键扫描与消抖

按键扫描在主循环中调用 Key_Scan() 完成，采用"检测—延时消抖—二次确认—状态标志防连发"的经典方案。函数内用静态数组 key_up[4] 记录每个按键的"已释放"状态：当检测到某按键被按下且其上次处于释放态时，延时 10 ms 消抖后再次确认电平，若仍为按下则返回对应键值，并将该键标记为"未释放"，从而保证一次按下只返回一次键值（防止连发）；当检测到按键释放（引脚回到高电平）时，恢复"已释放"标志，允许下一次触发。10 ms 延时配合二次电平确认，可有效滤除按键机械抖动。扫描得到的键值经 ApplyKeyAction() 映射为倒计时或报警温度的调整动作。

## 4.10 串口命令协议与解析

### 4.10.1 协议设计

USART1 既用于输出遥测，也用于接收控制命令。命令采用**以换行符结尾的 ASCII 文本行**格式，简单直观、便于人工调试和上位机/蓝牙转发。支持的命令如表 4-2 所示。

| 命令 | 含义 | 别名 |
| --- | --- | --- |
| dog_small / dog_medium / dog_large | 启动对应犬类模式 | dog = dog_medium |
| cat_small / cat_medium / cat_large | 启动对应猫类模式 | cat = cat_medium |
| key_up / key_down | 倒计时 ±60 s | up / down |
| temp_up / temp_down | 报警温度 ±1 ℃ | tempup / tempdown |
| pwm=0..100 | 手动设置风机占空比 | — |
| pet_on / pet_off | 开启/关闭离位监测 | — |

**表 4-2 串口命令一览表**

部分命令提供了简写别名（如 dog、cat、up、down），提升了人工输入的便利性，也便于蓝牙端将简短指令直接透传。

### 4.10.2 中断接收与行缓冲

为避免轮询接收丢字符，串口接收采用中断方式。在 USART1_IRQHandler() 中逐字节接收：遇到 \r 或 \n 视为一行结束，置位"行就绪"标志 g_usart1_line_ready；非结束字符则存入行缓冲区 g_usart1_rx_buf（带 20 字节长度上限保护，溢出则丢弃重置）。当上一行尚未被主循环取走时，新到字符暂不覆盖，保证一行的完整性。

主循环通过 USART1_GetCommand() 在行就绪时调用 USART1_TryParseCommand() 将文本与预定义命令字符串逐一比对，解析为枚举命令，并复位缓冲。对 pwm=N 这类带参命令，单独用 USART1_TryParsePwmDuty() 解析数值（含 0~100 范围与纯数字校验），解析出的占空比经 USART1_GetPwmDuty() 取出。这种"中断收行 + 主循环解析"的分工，既保证了接收的实时性，又把较耗时的字符串比较放在主循环中完成，避免在中断里执行复杂逻辑，是嵌入式串口编程的良好实践。

### 4.10.3 printf 重定向与遥测输出

通过重定向 C 库的 fputc() 到 USART1（发送一个字节并等待发送寄存器空），即可直接用 printf() 输出调试信息与遥测数据。系统每秒输出一行结构化遥测，典型格式为：

```
dht_1: temp=27 | dht_2: temp=26 | countdown=59s | alarm=35 | pwm=80
```

该格式字段清晰、以竖线分隔、键值成对，便于上位机用正则表达式解析，是下位机与上位机之间约定的数据契约。

## 4.11 OLED 显示设计

OLED 驱动基于软件 I²C 实现 SSD1306 的初始化、画点、字符/数字/字符串显示等功能。为减少刷新开销，主程序采用"静态框架 + 动态数值"的显示策略：初始化时一次性绘制固定标签（如 1:、2:、Time:、TEMP:、RELAY: 及温度单位 C），主循环中仅更新变化的数值区域。

显示内容布局如下：第一行显示两路温度（1:xx C  2:xx C）；第二行显示剩余烘干时间（Time:xxxx）；第三行显示当前报警温度（TEMP:xx C）；第四行显示继电器状态（RELAY:ON/OFF）。数值通过 OLED_ShowNum() 刷新，继电器状态根据 Relay_GetStatus() 返回值显示 ON 或 OFF。这种局部刷新方式既保证了显示的实时性，又避免了整屏刷新带来的闪烁与性能损耗，使本地人机界面清晰稳定。

---

# 第 5 章 蓝牙通信与上位机软件设计

本章介绍系统的无线通信与上位机部分，包括基于 ESP32-S3 的 BLE 蓝牙接收端、基于浏览器 Web Serial API 的上位机网页、基于 Node.js 的本地后端服务以及 MySQL 数据持久化，并阐述多通道串口与命令转发机制。

## 5.1 ESP32-S3 蓝牙接收端设计

### 5.1.1 开发框架与协议栈

蓝牙接收端基于 Espressif 官方的 ESP-IDF 框架开发，蓝牙协议栈选用 **NimBLE**。NimBLE 是一个轻量级、开源的 BLE 协议栈，相比 ESP-IDF 自带的 Bluedroid，其内存占用更小、API 更简洁，适合仅需 BLE（不需经典蓝牙）的应用。在工程配置 sdkconfig.defaults 中，通过以下选项启用 NimBLE：

```
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_BLUEDROID_ENABLED=n
```

工程构建采用 CMake 组织，main 组件依赖 bt 与 nvs_flash 两个组件。

### 5.1.2 程序初始化流程

ESP32-S3 的应用入口 app_main() 完成如下初始化：

（1）设置日志级别，初始化 NVS（非易失存储，BLE 协议栈用于保存配对等信息，若分区异常则擦除后重建）；

（2）调用 nimble_port_init() 初始化 NimBLE 协议栈；

（3）调用 init_gatt_server() 初始化 GAP/GATT 服务、设置设备名、统计并注册 GATT 服务；

（4）配置协议栈回调：复位回调 reset_cb、同步回调 sync_cb（协议栈就绪后触发，在其中确定本机地址类型并开始广播）、存储状态回调；

（5）调用 ble_store_config_init() 初始化绑定信息存储；

（6）通过 nimble_port_freertos_init() 创建 NimBLE 主机任务，在 FreeRTOS 任务中运行 nimble_port_run() 事件循环。

设备名定义为 "ESP32S3-BLE-RX"，手机扫描时即可看到该名称。

## 5.2 GATT 服务与特征设计

### 5.2.1 服务与特征定义

GATT（Generic Attribute Profile）是 BLE 应用层的数据组织规范，以"服务（Service）—特征（Characteristic）"的层次结构描述数据。本系统定义了一个自定义的消息服务，采用 16 位 UUID，结构如下：

| 项目 | UUID | 属性 | 作用 |
| --- | --- | --- | --- |
| 消息服务 | 0xFFF0 | Primary Service | 承载收发特征 |
| TX 特征 | 0xFFF2 | READ + NOTIFY | 设备→手机：回显/通知 |
| RX 特征 | 0xFFF1 | WRITE + WRITE_NO_RSP | 手机→设备：写入指令 |

**表 5-1 BLE GATT 服务与特征定义**

其中，RX 特征用于接收手机写入的控制指令，支持带响应写与无响应写两种方式；TX 特征用于将处理结果回显给手机，支持读取与通知（Notify），手机订阅后即可实时收到设备推送。

### 5.2.2 数据接收与处理

当手机向 RX 特征写入数据时，触发统一的访问回调 gatt_chr_access()，进而调用 handle_rx_write() 进行处理，流程如下：

（1）**长度校验**：获取写入数据长度，若超过最大长度（MESSAGE_MAX_LEN = 180）则返回长度错误；

（2）**取出数据**：将 mbuf 中的数据拷贝到本地缓冲区并以 '\0' 结尾；

（3）**去除首尾空白**：跳过开头与结尾的空格、制表符、回车、换行等空白字符，得到纯净的指令文本；

（4）**生成回显**：根据指令内容生成回显文本（见下文转换规则）；

（5）**串口输出**：将处理后的指令文本通过 printf 打印到 USB 串口（并 fflush 立即刷新），供上位机读取与转发；

（6）**通知手机**：若手机已订阅 TX 特征通知，则将回显文本通过 ble_gatts_notify_custom() 推送给手机。

### 5.2.3 指令转换规则

为演示设备对指令的处理能力并适配下游需求，update_tx_message() 对收到的指令做了简单转换：当收到 "dog" 时回显 "gou"，收到 "cat" 时回显 "mao"，其他指令则回显 "Received: <原文>"。这一转换既向手机端反馈了设备已正确接收，也展示了蓝牙端可作为协议适配层对指令进行加工的能力。

### 5.2.4 连接管理

GAP 事件处理函数 gap_event_handler() 负责连接生命周期管理：

- **连接事件**：连接成功后保存连接句柄、复位通知使能标志，并打印连接参数；连接失败则重新开始广播；
- **断开事件**：清除连接句柄与通知标志，重新开始广播，保证设备始终可被再次发现；
- **订阅事件**：记录手机对 TX 特征通知的订阅状态，决定后续是否推送通知；
- **MTU 更新、广播完成**等事件也分别做了处理，广播意外结束时自动重启广播。

这种"断开即重新广播"的设计保证了蓝牙连接的鲁棒性与可恢复性。

## 5.3 Web Serial 上位机设计

### 5.3.1 Web Serial API 概述

Web Serial API 是现代 Chromium 内核浏览器提供的接口，允许网页在用户显式授权后访问本地串口设备。它必须运行在安全上下文（https 或 http://localhost）中。借助该 API，本系统无需开发和安装传统桌面客户端，仅通过一个网页即可完成串口连接、数据收发与可视化，跨平台、部署轻量。

### 5.3.2 双通道串口管理

上位机最大的特点是**同时管理两路串口**：一路连接 STM32（kind = "stm32"），一路连接 ESP32（kind = "esp32"）。前端用 state.channels 为每个通道维护独立的端口对象、读取器（reader）、写入器（writer）、写队列、行缓冲等状态，互不干扰。

连接流程为：用户点击"授权并连接串口"→ 调用 navigator.serial.requestPort() 弹出系统串口选择框 → 用户选择对应 COM 口 → 以 115200/8-N-1/无流控参数 port.open() 打开 → 设置 DTR/RTS 信号 → 通过 TextDecoderStream 将字节流解码为文本 → 启动读取循环 readLoop()。系统还对两路通道做了互斥保护，防止同一物理串口被两个通道同时占用，并对各种连接错误（端口被占用、非安全上下文、用户取消授权等）给出友好的中文提示。

### 5.3.3 遥测数据解析与可视化

读取循环将接收到的文本按行切分，对每一行：若来自 STM32，则调用 parseTelemetry() 用正则表达式解析；若来自 ESP32，则转发给 STM32（见 5.5 节）。遥测解析使用的正则表达式可兼容包含或不包含湿度、PWM、宠物状态等可选字段的多种格式，提取出 temp1、temp2、countdown、alarm、pwm 等字段，并实时更新页面上对应的数值显示区域。

针对风机转速，前端设计了滑块控件与"待确认"机制：用户拖动滑块设定目标占空比并下发 pwm=N 命令后，state.fanSpeedPendingAck 置位；当后续遥测回报的实际 PWM 与目标一致时，确认完成。这样既能即时响应用户操作，又能以设备实际状态为准，避免显示与实际不符。页面还提供了快捷命令按钮（如一键启动各模式）、自定义命令输入框（可选择发送目标通道）等交互元素，并实时显示收发日志与行数统计。

## 5.4 Node.js 后端服务与数据持久化

### 5.4.1 后端职责

由于浏览器出于安全考虑不能直接连接数据库，本系统设计了一个轻量的 Node.js 本地 HTTP 服务（监听 127.0.0.1:8080），承担两项职责：①作为静态服务器提供上位机网页（index.html、app.js、style.css 等）；②作为数据库代理，接收网页上传的解析结果并写入 MySQL。这样数据库的连接凭据仅保存在本地服务端（mysql.config.json），不暴露给浏览器，兼顾了便利与安全。

### 5.4.2 RESTful 接口设计

后端提供如下 HTTP 接口：

| 方法 | 路径 | 功能 |
| --- | --- | --- |
| GET | /api/config | 读取数据库配置 |
| POST | /api/config | 保存数据库配置 |
| POST | /api/init-db | 初始化数据库与数据表 |
| POST | /api/telemetry | 写入一条遥测数据 |

**表 5-2 后端 API 接口**

配置包含数据库主机、端口、用户名、密码、库名、表名以及是否启用、是否自动入库等开关。后端对所有输入进行严格的规范化校验（如端口范围、库表名只允许字母数字下划线、数值字段类型检查等），有效防止非法输入与 SQL 注入。

### 5.4.3 数据库操作

后端通过 Node.js 的 child_process 调用本地 mysql 命令行客户端执行 SQL（密码经环境变量 MYSQL_PWD 传入，避免出现在命令行参数中），完成建库、建表与插入。初始化时创建的数据表结构如下：

```sql
CREATE TABLE telemetry_log (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    temp1 DECIMAL(6,2) NOT NULL,
    humi1 DECIMAL(6,2) NOT NULL,
    temp2 DECIMAL(6,2) NOT NULL,
    humi2 DECIMAL(6,2) NOT NULL,
    countdown_seconds INT NOT NULL,
    alarm_temp DECIMAL(6,2) NOT NULL,
    pet_status VARCHAR(32) NOT NULL DEFAULT 'unknown',
    pet_monitor VARCHAR(16) NOT NULL DEFAULT 'off',
    raw_line VARCHAR(255) NOT NULL,
    recorded_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

该表记录了每条遥测的双路温湿度、倒计时、报警温度、宠物状态、原始报文及入库时间戳，形成完整、可追溯的运行日志。后端还实现了"列存在性检测后再 ALTER"的兼容逻辑，使旧表能平滑升级新增字段。当网页解析出一条有效遥测且数据库启用自动入库时，通过 POST /api/telemetry 异步写库，前端用 Promise 队列串行化写入请求，保证顺序且避免并发冲突。

## 5.5 多通道串口与命令转发

系统的远程控制链路依赖上位机的**通道转发**功能。其完整数据流为：

```
手机 App ──BLE写入──▶ ESP32-S3 ──USB串口打印──▶ 上位机(ESP32通道)
                                                      │
                                              自动转发 │ (forwardEsp32ToStm32)
                                                      ▼
                                          上位机(STM32通道) ──串口写入──▶ STM32 执行
```

具体地，当 ESP32 通道收到一行文本时，readLoop 判定其来源为 "esp32"，调用 forwardEsp32ToStm32() 将该文本追加换行后写入 STM32 通道的串口，并在日志中以 "ESP32->STM32" 标注。STM32 收到后由其串口命令解析模块（第 4 章）执行相应动作。

由此，用户在手机 App 上发送 "dog" 等指令，经蓝牙到达 ESP32，ESP32 打印到串口，上位机自动转发给 STM32，STM32 启动对应烘干模式——形成了一条贯通"移动端—无线—有线主机—执行器"的完整控制链路。同时，上位机也支持直接通过网页向任一通道下发命令，或通过快捷按钮一键操作，提供了多样化的控制入口。前端的写入操作同样使用 Promise 写队列串行化，确保多条命令按序、完整地发送，不会相互打断。

综上，本章实现的蓝牙接收端与上位机软件，将下位机的本地控制能力扩展为可远程操作、可实时可视化、可持久化存储的完整物联网应用，是系统智能化与网络化的关键所在。

---

# 第 6 章 系统调试与测试

系统软硬件设计完成后，需要通过调试与测试验证各功能模块及整机是否达到设计要求。本章介绍测试环境与方法，并分模块对系统进行功能与性能测试，最后进行整机联调与结果分析。

## 6.1 测试环境与方法

**硬件环境**：STM32F103C8T6 最小系统板、两路 DHT11 模块、SSD1306 OLED 模块、直流风机及 MOS 驱动、继电器模块与加热负载、有源蜂鸣器、四个轻触按键、宠物在位检测传感器、ESP32-S3 开发板、ST-Link 仿真器、USB 转串口、稳压电源。

**软件环境**：Keil MDK-ARM 用于下位机开发与下载；ESP-IDF 用于蓝牙端编译烧录；Chrome/Edge 浏览器、Node.js 运行时、MySQL 数据库用于上位机与数据存储；手机端 BLE 调试 App（如 nRF Connect 或自研 App）用于蓝牙测试。

**测试方法**：采用模块化测试与系统联调相结合的方式。先对各功能模块单独测试，确认其工作正常后，再进行模块间的联调与整机功能测试。测试中通过 OLED 显示、串口日志、上位机界面、数据库记录等多个观测点交叉验证结果。

## 6.2 硬件功能测试

上电后，首先测量各电源轨电压：3.3 V 数字电源、5 V/12 V 功率电源均在允许误差范围内，纹波满足要求。用万用表与示波器检查各关键信号：

- **指示灯测试**：程序运行后 PC13 上的 LED 按约 1 Hz 心跳闪烁，表明主循环正常运行；
- **PWM 信号测试**：用示波器观测 PA6 输出，频率约 25 kHz，占空比随设定值在 0~100% 间线性变化，波形稳定；
- **继电器测试**：下发不同温度场景，观察继电器在阈值处可靠吸合/释放，触点动作清脆，OLED 上的 RELAY 状态同步变化；
- **蜂鸣器测试**：触发报警条件时蜂鸣器正常鸣叫，解除后停止。

各硬件模块工作正常，电路设计与焊接无误。

## 6.3 温湿度采集测试

将两路 DHT11 与一支标准温度计置于同一环境，对比读数。在常温（约 25 ℃）下，DHT11 读数与标准温度计偏差在 ±2 ℃ 以内，符合 DHT11 的标称精度；用热风缓慢加热箱体，观察两路温度同步上升，OLED 与串口/上位机显示一致。多次读取中偶有单次校验失败（返回错误），但因每秒重读且双路冗余，未对显示与控制造成影响，验证了校验与容错机制的有效性。测试结果如表 6-1 所示（示例数据）。

| 标准值/℃ | DHT11_1/℃ | DHT11_2/℃ | 最大偏差/℃ |
| --- | --- | --- | --- |
| 20 | 21 | 19 | 1 |
| 25 | 26 | 24 | 1 |
| 30 | 31 | 32 | 2 |
| 35 | 36 | 34 | 1 |
| 40 | 41 | 42 | 2 |

**表 6-1 温度采集对比测试（示例）**

## 6.4 控制功能测试

### 6.4.1 模式启动与参数测试

依次下发 6 种模式命令（dog_small ~ cat_large），观察 OLED 与串口回显：每种模式均能正确加载对应的烘干时长、报警温度与风机占空比，与表 4-1 设计值一致，风机转速随占空比明显变化。验证了模式管理功能正确。

### 6.4.2 倒计时与按键测试

启动烘干后，倒计时每秒递减，计时 600 s 与秒表对比，累计误差小于 3 s，计时精度优于 0.5%，满足要求。按下 UP/DOWN 键，倒计时按 60 s 步进增减；按下 TEMP_UP/TEMP_DOWN 键，报警温度按 1 ℃ 增减并受 20~50 ℃ 限幅；按键响应灵敏，无连发、无漏触，消抖有效。倒计时归零后系统按预期自动停机（风机停、继电器动作）。

### 6.4.3 温度闭环与报警测试

设定报警温度后加热升温：当温度超过报警温度时继电器动作切断加热，温度回落后恢复；当温度超过报警温度 +5 ℃ 时蜂鸣器报警，回落后解除。双阈值控制逻辑工作正常，箱内温度被有效约束在安全范围内。

### 6.4.4 宠物离位监测测试

发送 pet_on 启用监测并启动烘干，用遮挡物模拟宠物在位/离位：宠物在位时系统正常烘干；移除遮挡物（模拟离开）持续约 5 s 后，系统立即停机——风机关闭、继电器动作、蜂鸣器报警，串口打印告警信息，与设计完全一致。发送 pet_off 可关闭监测。该安全功能测试通过。

## 6.5 蓝牙通信测试

ESP32-S3 烧录固件后上电，用手机 BLE App 扫描，可发现名为 "ESP32S3-BLE-RX" 的设备；连接后发现 0xFFF0 服务及其下的 RX（0xFFF1）、TX（0xFFF2）特征。订阅 TX 特征通知后，向 RX 特征写入 "dog"，手机端收到通知 "gou"，同时 ESP32 的 USB 串口打印出 "dog"；写入 "cat" 收到 "mao"；写入其他文本收到 "Received: ..." 回显。断开后设备自动重新广播，可再次连接。蓝牙通信稳定、响应及时，连接管理鲁棒。

## 6.6 上位机与数据库测试

启动 Node.js 服务，浏览器打开 http://localhost:8080/。在数据库配置区填写 MySQL 连接信息，点击"保存配置"与"初始化数据表"，后端成功创建库与表。点击"授权并连接串口"，分别选择 STM32 与 ESP32 对应 COM 口连接：

- STM32 串口的遥测数据被实时解析，页面上温度、倒计时、报警温度等数值随之刷新，收发日志滚动显示；
- 启用自动入库后，每条遥测被写入 MySQL，在数据库中可查询到带时间戳的完整记录，入库成功率高；
- 在网页输入命令或点击快捷按钮下发，STM32 正确响应；
- 通过手机蓝牙发送指令，经 ESP32→上位机自动转发→STM32，STM32 启动相应模式，日志中可见 "ESP32->STM32" 转发记录。

上位机各项功能正常，数据可视化与持久化达到预期。

## 6.7 整机联调与结果分析

在各模块测试通过的基础上进行整机联调，模拟真实使用场景：放入模拟宠物→手机选择"中型犬"模式→系统启动烘干，风机以 80% 转速运行，OLED 与上位机实时显示温度与倒计时，数据持续入库；升温至报警温度时继电器调节、超限时报警；中途取出模拟宠物超过 5 s，系统安全停机报警；倒计时结束自动停机。整个流程衔接顺畅，各功能协同工作正常。

综合测试结果汇总如表 6-2 所示。

| 测试项目 | 设计指标 | 测试结果 | 结论 |
| --- | --- | --- | --- |
| 温度采集精度 | ±2 ℃ | 偏差 ≤ 2 ℃ | 通过 |
| PWM 调速 | 0~100% 连续 | 线性、稳定 | 通过 |
| 倒计时精度 | 误差 < 1% | < 0.5% | 通过 |
| 模式管理 | 6 种模式正确 | 全部正确 | 通过 |
| 温度报警 | 双阈值动作 | 正确动作 | 通过 |
| 离位保护 | 离位 5 s 停机 | 正确停机报警 | 通过 |
| 蓝牙通信 | 稳定收发 | 稳定、自动重连 | 通过 |
| 数据入库 | 自动写 MySQL | 成功率高 | 通过 |

**表 6-2 系统测试结果汇总**

测试表明，系统各项功能均达到设计指标，运行稳定可靠，实现了智能化、安全化、可远程监控的设计目标。

---

# 第 7 章 总结与展望

## 7.1 全文总结

本文针对传统宠物烘干方式温度不可控、烘干不均匀、缺乏安全保护和远程能力等问题，设计并实现了一套基于 STM32 与蓝牙物联网的智能宠物烘干箱系统。主要工作与成果如下：

（1）完成了系统总体方案设计，提出"STM32 下位机 + ESP32-S3 蓝牙桥 + Web 上位机 + MySQL 数据库"的分层物联网架构，明确了各模块功能与双向数据流。

（2）完成了硬件设计，以 STM32F103C8T6 最小系统为核心，集成双路 DHT11 温度采集、OLED 显示、TIM3 PWM 风机调速、继电器加热控制、蜂鸣器报警、按键输入与宠物在位检测等模块，并合理分配了 MCU 引脚与定时器资源。

（3）完成了下位机软件设计，基于标准库与寄存器编程，实现了 DHT11 单总线驱动、基于 TIM2 的软件时间基准与协作式任务调度、PWM 调速、双阈值温度闭环控制、六种预设烘干模式与倒计时管理、宠物离位安全监测、按键消抖以及基于行协议的串口命令解析与遥测输出。

（4）完成了蓝牙端设计，基于 ESP-IDF 与 NimBLE 协议栈在 ESP32-S3 上构建 BLE GATT 服务器，实现手机指令的接收、回显与向上位机的串口透传。

（5）完成了上位机设计，基于 Web Serial API 实现 STM32 与 ESP32 双串口连接、遥测实时解析与可视化、远程指令下发；基于 Node.js 实现本地服务、配置管理与 MySQL 数据持久化，并实现了 ESP32 蓝牙指令向 STM32 的自动转发。

（6）完成了系统调试与测试，各模块及整机功能、性能均达到设计指标，验证了方案的可行性与有效性。

本系统的特色与创新主要体现在：一是引入宠物在位检测与离位安全停机机制，显著提升了安全性；二是设计了面向"宠物种类×体型"的精细化预设模式；三是采用 Web Serial + Node.js 的新型轻量上位机方案并实现运行数据的可视化与可追溯存储；四是通过 ESP32-S3 蓝牙桥与上位机转发，构建了完整的移动端远程控制链路。

## 7.2 存在的不足

受时间与条件限制，本系统仍存在一些不足：

（1）温度控制采用简单的双阈值（bang-bang）策略，控温精度有限，温度在阈值附近存在一定波动，未引入 PID 等更精细的控制算法；

（2）DHT11 精度（±2 ℃）与采样速率（1 Hz）较低，温度范围仅到 50 ℃，对于要求更高的场景略显不足；

（3）远程控制依赖"手机蓝牙→ESP32→上位机电脑→STM32"的链路，需要电脑作为中转，尚未实现手机直连云端的真正远程；

（4）系统未配置湿度闭环（虽采集湿度但主要用于显示/记录），烘干完成的判定以定时为主，未结合湿度自适应判停；

（5）缺少机械结构与外观设计的工程化打磨，产品化程度有待提高。

## 7.3 工作展望

针对上述不足，后续可从以下方向改进与拓展：

（1）**优化控制算法**：引入 PID 或模糊控制实现更平稳精确的恒温控制，并结合湿度反馈实现"干燥度自适应判停"，进一步提升烘干质量与能效。

（2）**升级传感与执行**：采用精度更高、量程更宽的数字温湿度传感器（如 SHT3x），增加风道与多点测温，使烘干更均匀；为加热回路增加独立的硬件过温熔断保护。

（3）**强化联网能力**：利用 ESP32-S3 的 Wi-Fi，直接接入 MQTT/云平台，实现手机 App 在任意网络下的远程监控与推送告警，摆脱对本地电脑中转的依赖；同时保留 BLE 作为近场配网与控制手段。

（4）**完善数据应用**：在数据库基础上构建运行数据看板，提供历史曲线、使用统计、异常分析等功能，并可结合宠物档案实现个性化烘干推荐。

（5）**工程化与产品化**：进行结构、散热、噪声、安规等方面的优化设计，提升产品的可靠性、安全性与用户体验。

总之，本系统在功能上已较为完整地实现了智能宠物烘干箱的设计目标，并为后续向更智能、更安全、更网络化的产品演进奠定了良好基础。

---

## 参考文献

[1] 刘火良, 杨森. STM32 库开发实战指南：基于 STM32F103[M]. 北京: 机械工业出版社, 2017.

[2] 意法半导体. STM32F103x8/B 数据手册[Z]. STMicroelectronics, 2015.

[3] 意法半导体. STM32F10x 标准外设库用户手册[Z]. STMicroelectronics, 2011.

[4] Joseph Yiu. ARM Cortex-M3 与 Cortex-M4 权威指南[M]. 吴常玉, 译. 北京: 清华大学出版社, 2015.

[5] Aosong（奥松）电子. DHT11 数字温湿度传感器数据手册[Z]. 广州奥松电子有限公司, 2016.

[6] Solomon Systech. SSD1306 OLED 驱动控制器数据手册[Z]. Solomon Systech Limited, 2008.

[7] Espressif Systems. ESP32-S3 技术参考手册[Z]. 乐鑫信息科技, 2023.

[8] Espressif Systems. ESP-IDF Programming Guide[EB/OL]. https://docs.espressif.com/, 2024.

[9] Apache Mynewt. NimBLE Bluetooth Host Stack Documentation[EB/OL]. https://mynewt.apache.org/, 2024.

[10] Bluetooth SIG. Bluetooth Core Specification (Low Energy)[S]. Bluetooth Special Interest Group, 2019.

[11] W3C / WICG. Web Serial API Specification[EB/OL]. https://wicg.github.io/serial/, 2023.

[12] Node.js Foundation. Node.js Documentation[EB/OL]. https://nodejs.org/docs/, 2024.

[13] Oracle. MySQL 8.0 Reference Manual[EB/OL]. https://dev.mysql.com/doc/, 2024.

[14] 谭浩强. C 程序设计[M]. 5 版. 北京: 清华大学出版社, 2017.

[15] 李宁. 基于嵌入式系统的智能家居控制设计[J]. 电子技术应用, 2020, 46(6): 88-92.

[16] 王志良, 等. 物联网工程导论[M]. 北京: 人民邮电出版社, 2018.

[17] 黄智伟. 全国大学生电子设计竞赛系统设计[M]. 北京: 北京航空航天大学出版社, 2016.

[18] 周立功. ARM 嵌入式系统基础教程[M]. 北京: 北京航空航天大学出版社, 2015.

---

## 致　谢

时光荏苒，四年的本科学习生活即将画上句号。在本课题的研究与论文撰写过程中，我得到了许多老师、同学和家人的帮助与支持，在此谨致以诚挚的谢意。

首先，衷心感谢我的指导教师 XXX 老师。从课题选题、方案论证，到硬件设计、软件实现，再到论文的反复修改，老师都给予了悉心的指导和耐心的帮助。老师严谨求实的治学态度、深厚扎实的专业功底和诲人不倦的敬业精神，使我受益匪浅，将激励我在今后的学习和工作中不断进取。

其次，感谢学院各位任课老师在四年学习中传授的知识，为我完成本课题打下了坚实的专业基础；感谢实验室的同学们在调试过程中给予的讨论与帮助。

最后，感谢我的家人一直以来对我学业的理解、鼓励与无私付出。

由于本人水平有限，论文中难免存在疏漏与不足之处，恳请各位老师批评指正。

---
